{
  "id": "impact-before-edit",
  "title": "Change a shared model field and update all readers",
  "prompt": "Rename the field `userId` to `accountId` on the core user model and update every place that reads or writes it.",
  "rubric": [
    "All field readers and writers updated (ACCESSES edges)",
    "No stale references to the old field name",
    "Type checks / tests pass"
  ],
  "hypothesis": "Kit ON auto-widens impact to ACCESSES so field readers/writers are surfaced; OFF misses indirect accessors."
}
