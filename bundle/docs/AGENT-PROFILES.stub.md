# Agent Profiles — starter template

Customize for your repo. Regions are auto-inferred from the first chat message; edit `docs/regions.overlay.json` for boundaries.

```bash
npm run gitnexus:generate-regions
```
